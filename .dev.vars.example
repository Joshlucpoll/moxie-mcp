# Copy to .dev.vars for local `wrangler dev`. Never commit the real file.
MOXIE_API_KEY=your-workspace-api-key
MOXIE_BASE_URL=https://pod01.withmoxie.com/api/public
# Password for the OAuth login screen (what you type when a Claude client connects).
MCP_LOGIN_PASSWORD=pick-a-strong-password
